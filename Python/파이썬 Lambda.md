- 메모리 절약, 가독성 향상, 코드 간결
- **But** 과용하면 오히려 가독성 감소
- 일반 함수는 객체 생성 후 리소스(메모리)를 할당하는 반면 람다는 즉시 실행 함수라서 Heap초기화 후 메모리를 초기화한다
- 익명 함수라서 변수에 담아서 써야 한다
- #### 기본 형식
	- lambda x, y : x * y

- ##### 예시
	- lambda_mul_func = lambda x, y : x * y
	- print(lambda_mul_func(20, 20))